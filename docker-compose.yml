version: '3'
services:
  db:
    image: mysql:8.0
    platform: linux/amd64
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: portfolio
  app:
    image: golang:1.18
    volumes:
      - .:/app
      - gomod:/go/pkg/mod
    working_dir: /app
    depends_on:
      - db
volumes:
  gomod:
